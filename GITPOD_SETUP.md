# PhoneK App - Gitpod Quick Start

شرح سريع لاستخدام Gitpod:

## 🚀 الخطوات:

1. **افتح الرابط:**
   ```
   https://gitpod.io/#https://github.com/alhmeemzool-ops/phonek_app
   ```

2. **انتظر التثبيت** (يستغرق ~5 دقائق)

3. **شغّل التطبيق:**
   ```bash
   flutter run -d web
   ```

4. **افتح في المتصفح:**
   - سيظهر رابط مثل: `http://localhost:5000`
   - اضغط عليه أو افتحه مباشرة

## 📝 الأوامر المهمة:

```bash
# تحديث المكتبات
flutter pub get

# تشغيل التطبيق على الويب
flutter run -d web

# إعادة تحميل ساخن
r  # اضغط R بعد التشغيل

# إعادة تشغيل كاملة
R  # اضغط Shift+R
```

## ✨ المميزات:

- ✅ لا تحتاج تثبيت شيء
- ✅ كل شيء في المتصفح
- ✅ تحديثات فورية
- ✅ سهولة المشاركة

---

**جرّب الآن!** 🎉
